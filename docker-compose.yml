version: "3.8"
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: mohsi_user
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: picscale_db
    ports:
      - "5433:5432" 
